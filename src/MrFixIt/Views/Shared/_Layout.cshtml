<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link rel="stylesheet" href="~/css/styles.css" type="text/css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>

    <script src="~/js/scripts.js"></script>
     <title>@ViewBag.Title</title>
</head>
<body>
    @*//Navbar*@


    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">MR. FIX-IT</a>
            </div>
            <ul class="nav navbar-nav">
                <li class="active"><a href="/">HOME</a></li>
                <li><a href="/About/Index">ABOUT US</a></li>
                <li><a href="/Jobs/Index/">HIRE US</a></li>

            @if (User.Identity.IsAuthenticated)
            {
                <li>
                    @Html.ActionLink("WORKER DASHBOARD", "Index", "Workers")
                </li>
            }

            @if (User.Identity.IsAuthenticated)
            {
                <li>
                    @Html.ActionLink("LOG OUT", "LogOff", "Account")
                </li>
            }
                </ul>

            @*@if (User.Identity.IsAuthenticated)
            {
                @using (Html.BeginForm("LogOff", "Account"))
                {
                    <input type="submit" class="btn btn-default btn-lg pull-right heading-button" value="Log out" />
                }
            }
            else
            {
                <ul class="nav navbar-nav navbar-right">
                    <li><a href="/Account/Login"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
                    <li><a href="/Account/Register"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
                </ul>

            }*@
        </div>
    </nav>


    @*Current User Dashboard*@



    <div class="container">
        @RenderBody()
    </div>
</body>
</html>
